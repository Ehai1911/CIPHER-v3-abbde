# CIPHER — Список задач по улучшению

> Обновляется по мере выполнения. Статусы: ⬜ Не начато | 🔄 В процессе | ✅ Готово

---

## 🔍 БЛОК 1 — Улучшение поиска конкурентов (api/analyze.js)

### Быстрые правки (~ 1-2 часа)

- ✅ **1.1** Подключить `searchGoogle()` в discover mode — добавлена параллельно к DDG + Brave для CIS и глобального поиска.

- ✅ **1.2** Увеличить лимит текста для AI с 4000 до 8000 символов — теперь AI видит результаты всех запросов.

- ✅ **1.3** Увеличить лимит возвращаемых конкурентов с 8 до 15.

- ✅ **1.4** Добавить Яндекс поиск для СНГ — новая функция `searchYandexRaw()`, запускается параллельно с DDG/Google/Brave.

- ✅ **1.5** Удалить переменные `aiDebugOut` и `aiDebug` из кода.

### Средние правки (~ 2-4 часа)

- ✅ **1.6** Пре-скрап найденных конкурентов: после AI запускаем `fetchJina()` параллельно на все URL, получаем 300 символов превью с реальным контентом сайта. Добавлено поле `preview` к каждому конкуренту.

- ✅ **1.7** Валидация живых сайтов через пре-скрап: если `fetchJina()` вернул < 100 символов — сайт мёртвый, убираем из результатов. Объединено с задачей 1.6.

- ✅ **1.8** Дедупликация по домену после AI: `seenDomains` Set фильтрует одинаковые сайты найденные в разных источниках (DDG/Яндекс/Google/Brave).

- ✅ **1.9** Расширены запросы: для СНГ добавлены `топ ${areaShort} ${geoStr}` и `лучшие ${areaShort} компании ${geoStr}`. Для глобального рынка: `top ... companies` и `best ...`. DDG теперь использует 4 запроса вместо 3. Яндекс получает топ-запрос.

---

## 🔗 БЛОК 2 — Связать Margo и CIPHER

- ✅ **2.1** Проанализировали "импорт из Cipher" в Margo — читает из `localStorage['neurovia_state']` competitor URLs (одностороннее, CIPHER → Margo).

- ✅ **2.2** Обратная передача данных Margo → CIPHER: `renderSiteData/Reviews/AdsData()` теперь вызывают `saveMargoData()` → сохраняют в `localStorage['margoAnalysis']`. CIPHER читает через `getMargoContext()` и передаёт в API как `margoContext`.

- ⬜ **2.3** Автозапуск Margo-скрапа из discover mode — пока пропускаем, достаточно что пользователь запускает Margo вручную перед анализом.

- ✅ **2.4** `margoContext` добавлен в промпты табов: reputation, offers, channels, market, gaps, swot, strategy, quickwins — помечен как "приоритетный источник данных".

---

## 🖥️ БЛОК 3 — UI улучшения (cipher/index.html)

- ✅ **3.1** Добавить экран подтверждения конкурентов между discover и анализом. Реализовано: после поиска пользователь видит список конкурентов с чекбоксами, выбирает нужных и нажимает "Анализировать N →".

- ✅ **3.2** На экране подтверждения показывать превью контента с сайта конкурента (поле `preview` из пре-скрапа, задача 1.6). Также показывает `why` поле если preview нет.

- ✅ **3.3** Прогресс поиска по источникам: DuckDuckGo → Яндекс → Google → Brave появляются с задержкой (анимация пульса), при завершении все становятся зелёными. Заголовок меняется с "Ищем конкурентов" на "Анализируем рынок" после discover.

---

## 🏗️ БЛОК 4 — Hub и Local Agent

- ✅ **4.1** Hub добавлен в репозиторий как `hub/index.html`.

- ✅ **4.2** Routing исправлен: онлайн → `/?params` (корень = CIPHER), офлайн → `/local/index.html?params`.

- ✅ **4.3** Создан `local/index.html` — офлайн агент с опросником (бизнес/город/цена), поиском конкурентов через discover API, экраном подтверждения и анализом. Показывает карточки с адресом, рейтингом, превью.

- ✅ **4.4** Интегрировать 2GIS API напрямую — добавлен отдельный режим `discover-local` в analyze.js. Новые функции `search2GIS()` (с маппингом город→slug) и `searchYandexMaps()`. Параллельный поиск: 2GIS + Яндекс.Карты + Яндекс.Поиск + DDG. AI извлекает name/address/phone/rating/reviews. Local agent обновлён на `tab: 'discover-local'`, прогресс показывает 2GIS → Яндекс.Карты → Яндекс.Поиск.

---

## 🤖 БЛОК 6 — Оптимизация AI моделей (наш ключ, наши расходы)

> Цель: убрать BYOK, использовать наши ключи, снизить стоимость одного анализа с ~$0.05 до ~$0.008

### Архитектура
- Discover (извлечение URL): **Z.ai GLM-4.7-Flash** → бесплатно
- Простые табы (9 шт): **DeepSeek V3** → ~$0.0002/таб
- Аналитические табы strategy/swot/quickwins/gaps (4 шт): **Claude Haiku** → качество важнее
- Fallback цепочка на каждом уровне если провайдер недоступен

### Задачи

- ✅ **6.1** Вынести вызов AI в единую функцию `callAI(provider, apiKey, systemPrompt, userPrompt, maxTokens)` — убрать дублирование Anthropic/OpenAI кода из 3 мест в analyze.js

- ✅ **6.2** Добавить поддержку DeepSeek V3 (`api.deepseek.com`, модель `deepseek-chat`, OpenAI-совместимый формат)

- ✅ **6.3** Добавить поддержку Z.ai GLM-4.7-Flash (`open.bigmodel.cn`, модель `glm-4-flash`, OpenAI-совместимый формат)

- ✅ **6.4** Роутинг задач по моделям: discover → Z.ai, простые табы → DeepSeek, strategy/swot/quickwins/gaps → Haiku

- ✅ **6.5** Fallback цепочка: discover [Z.ai→DeepSeek→Haiku→OpenAI], simple [DeepSeek→Haiku→OpenAI], complex [Haiku→DeepSeek→OpenAI]. Пропуск если ключ не задан, retry на следующем при ошибке или невалидном JSON.

- ✅ **6.6** Поля ввода API ключа в `index.html` не было — ключи передавались через `state.data`. Убраны устаревшие сообщения об ошибках с упоминанием ключа, заменены на нейтральные ("Сервис временно недоступен").

- ✅ **6.7** Добавить в `.env`: `ZAI_API_KEY`, `DEEPSEEK_API_KEY`, `ANTHROPIC_API_KEY`. Инструкция для Vercel.

- ✅ **6.8** Обновить `discover-local` (2GIS режим) — переключён на `callAIWithFallback('discover', ...)`

- ✅ **6.9** Код проверен: все 3 пути вызова AI (fetchTab, discover, discover-local) используют callAIWithFallback. Синтаксис корректен. Fallback цепочки покрывают все случаи включая отсутствие ключа.

_Последнее обновление: 2026-04-08 (блок 6 выполнен полностью)_

---

## 🗄️ БЛОК 5 — Supabase хранилище (будущее)

- ⬜ **5.1** Спроектировать схему БД: таблицы `clients`, `competitors`, `analyses`.

- ⬜ **5.2** Подключить Supabase JS SDK.

- ⬜ **5.3** После discover — сохранять найденных конкурентов в Supabase с привязкой к client_id.

- ⬜ **5.4** Повторный анализ — загружать конкурентов из Supabase, не искать заново.

- ⬜ **5.5** История анализов — клиент видит все предыдущие запуски.

---

## ✅ Выполнено

- ✅ Заменить Bing на DuckDuckGo в discover mode (DDG через Jina Reader, kl=kz-kz для Казахстана)
- ✅ Исправить нормализацию URL (добавить `https://` если отсутствует)
- ✅ Сократить запросы для CIS: `areaShort = area.split(' ').slice(0,2).join(' ')`
- ✅ Создать `hub/index.html` — опросник с роутингом онлайн/офлайн/смешанный
- ✅ Исправить баг в hub: тип "смешанный" теперь корректно показывает офлайн-форму
- ✅ Создать `.env` файл чтобы vercel dev видел OPENAI_API_KEY
- ✅ Создать `.claude/launch.json` для запуска сервера

---

_Последнее обновление: 2026-04-08 (блок 6 выполнен)_
